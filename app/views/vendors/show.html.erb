<h3> Vendor Show Page: </h3>
<label> Vendor Name: </label>
<%= @vendor.name %>
<br>
<label> Sweets They Sell: </label> 
<% @vendor.sweets.each do |sweet|%>
<%= link_to sweet.name, sweet_path(sweet) %>
<% end %>

