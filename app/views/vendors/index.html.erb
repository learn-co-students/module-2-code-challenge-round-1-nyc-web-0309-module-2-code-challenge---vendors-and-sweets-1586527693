<h1>All Vendors</h1>

<ul>
  <% @vendors.each do |vendor| %>
    <li><%= link_to vendor.name, vendor %></li>
  <% end  %>
</ul>

<%= button_to "Add a new pairing!", new_vendor_sweet_path, method: :get%>