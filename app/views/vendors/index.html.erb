<h1>All Vendors</h1>
Average Price For All Sweets:
<% @array = []%>
<% @vendors.each do |vendor| %>
  <% vendor.vendor_sweets.each do |vs| %>
        <% @array << vs.price %>
  <% end %>
<% end %> 
<% @array%>
$<%= @array.sum(0.0) / @array.size%> <br>
-------Bonus Attempt Above------- <br>


<h2>ALL VENDORS BELOW</h2>
<ul>
  <% @vendors.each do |vendor| %>
    <li><%= link_to vendor.name, vendor_path(vendor) %></li>
  <% end  %>
</ul>

###

