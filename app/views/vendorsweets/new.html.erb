<h1>hi buddy</h1>

<% if @vendorsweets.errors.any? %>
    <% @vendorsweets.errors.full_messages.each do |error| %>
        <%= error %>
    <% end %>
<% end %>

<%= form_for @vendorsweets do |f| %>
    <%= f.label :name %>
    <%= f.collection_select :vendor_id, Vendor.all, :id, :name %>
    <%= f.label :name %>
    <%= f.collection_select :sweet_id, Sweet.all, :id, :name %>
    <%= f.label :price%>
    <%= f.number_field :price%>

    <%= f.submit %>

<% end %>