<% if flash[:errors] %>
<% flash[:errors].each do |error| %>
    <%= error %>
    <% end %>
    <% end %>
<%= form_for @vendorsweet do |f|%>
<%= f.collection_select(:vendorsweet, :sweet_id, Sweet.all, :id, :name)%>
<%= f.collection_select(:vendorsweet, :vendor_id, Vendor.all, :id, :name)%>
<%= f.number_field "Price"%>
<%= f.submit %>
<%= %>
<% end%>