<h1>New Form </h1>
<% if flash[:errors] %>
<ul>
    <% flash[:errors].each do |error|%>
        <li><%= error %></li>
        <% end %>
</ul>
<% end %>

Please select your sweet
<%= form_for @vendor_sweet do |f| %>
<%= f.label :price %>
<%= f.text_field :price %>

<%= f.collection_select :vendor_id, @vendors, :id, :name %>
<%= f.collection_select :sweet_id, @sweets, :id, :name %><br>
<%= f.submit %>
<% end %>